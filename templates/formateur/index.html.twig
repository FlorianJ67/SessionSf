{% extends 'base.html.twig' %}

{% block title %}Liste des Formateurs{% endblock %}

{% block body %}
    <div id="formateurList">
        {% for formateur in formateurs %}
            <div>
                <div></div>
                <div>
                    <a href="">Formateur {{ formateur }}</a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
