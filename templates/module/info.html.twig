{% extends 'base.html.twig' %}

{% block title %}Info module{% endblock %}

{% block body %}
    <div id="moduleInfo">
            <div>
                <a href="{{ path('info_module', {'id': module.id}) }}">{{ module.name }}</a>
            </div>
            <div>
                <p>Catégorie: {{ module.categorie.name }}</p>
               
            </div>
    </div>

{% endblock %}
